import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  inputWrapper: {
    position: 'relative',
    width: '100%',
  },

  inputArea: {
    width: '100%',
    height: 54,
    backgroundColor: '#F5F5F5',
    fontSize: 16,
    borderRadius: 16,
    padding: 14,
    paddingRight: 40,
  },

  iconBox: {
    position: 'absolute',
    right: 14,
    top: 14,
  },
});
