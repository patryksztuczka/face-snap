import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  errorMessageWrapper: {
    flexDirection: 'row',
    gap: 6,
    alignItems: 'center',
  },

  errorMessageText: {
    color: '#E54335',
  },
});
