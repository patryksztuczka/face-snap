import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  policyCheckboxWrapper: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
  },

  description: {
    fontSize: 16,
  },
});
