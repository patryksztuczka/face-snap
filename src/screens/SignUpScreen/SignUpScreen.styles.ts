import { StyleSheet } from 'react-native';

export const signUpScreenStyles = StyleSheet.create({
  signUpScreenWrapper: {
    width: '100%',
  },

  title: {
    marginTop: 32,
    marginBottom: 8,
    fontSize: 40,
  },

  paragraph: {
    fontSize: 16,
    marginBottom: 32,
  },

  inputsWrapper: {
    width: '100%',
    gap: 12,
    marginBottom: 32,
  },
});
